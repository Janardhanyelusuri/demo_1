apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cm-backend.fullname" . }}-test-connection"
  labels:
    {{- include "cm-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cm-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
