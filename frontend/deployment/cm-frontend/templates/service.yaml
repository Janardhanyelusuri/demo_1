apiVersion: v1
kind: Service
metadata:
  name: {{ include "cm-frontend.fullname" . }}
  labels:
    {{- include "cm-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cm-frontend.selectorLabels" . | nindent 4 }}
